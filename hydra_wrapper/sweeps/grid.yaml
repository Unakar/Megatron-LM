# Sweep grid: define parameters to sweep
# Format: key: [value1, value2, ...] for each parameter
# Nested keys use dot notation matching Hydra config structure

optimizer.lr: [0.0005, 0.001, 0.002]
optimizer.momentum: [0.85, 0.9, 0.95]
